import util.GetPosters;

public class TestPosters {
    public static void main(String[] args) {
        String testGameName = "halo";
        System.out.println("GetPosters.getImagePoster(testGameName) = " + GetPosters.getImagePoster(testGameName));
    }
}